{
  "name": "graphql",
  "scripts": {
    "dev": "tsx watch src/server/index.ts",
    "build": "npx tsc",
    "clean": "rm -rf ./build",
    "release": "release-it --ci"
  },
  "dependencies": {
    "@fastify/cors": "^8.4.1",
    "@template/constant-definitions": "workspace:*",
    "fastify": "^4.24.3"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "tsx": "^4.1.1",
    "typescript": "^5.2.2"
  }
}
